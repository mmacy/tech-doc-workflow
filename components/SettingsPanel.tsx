// This component is being phased out and its functionality moved to
// components/settings/AgentSettingsTab.tsx and pages/SettingsPage.tsx.
// Keeping the file for now to avoid breaking imports during transition,
// but it should not be used directly.

import React from 'react';

const DeprecatedSettingsPanel: React.FC = () => {
  return null; 
};

export default DeprecatedSettingsPanel;
