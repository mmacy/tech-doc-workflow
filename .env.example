# API Keys are now managed at runtime for security
# Do not add API keys to this file or commit them to Git
# 
# Legacy variables (remove these if they exist in your .env file):
# VITE_OPENAI_API_KEY=your-key-here  # DEPRECATED - Use runtime key management
# VITE_GEMINI_API_KEY=your-key-here  # DEPRECATED - Use runtime key management
# VITE_AZURE_OPENAI_API_KEY=your-key-here  # DEPRECATED - Use runtime key management
#
# How to add API keys:
# 1. Run the development server: npm run dev
# 2. Open the Settings page in the application
# 3. Navigate to the "LLM Provider" tab
# 4. Enter your API keys using the secure key management interface
#
# Keys are stored in memory only and never written to disk.